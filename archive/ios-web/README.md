# Archived iOS web reader

Parked. Phones on the live site go to `/get-app.html`. Native app: `PibliaIOS/`.

This folder is the YouVersion-style PWA we built first (tabs, focus, L/R cycle, notes, bookmarks, KJV). Do not delete. Use it as the spec when writing Swift.
